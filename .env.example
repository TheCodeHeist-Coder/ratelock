# Variables consumed by docker-compose (copy to .env to override defaults).
# `docker compose up` works without this file thanks to built-in defaults.

REDIS_PASSWORD=thisistheredislocalpassword
JWT_SECRET=change-me-in-production

# Public origin the browser hits the web container on (used for backend CORS).
FRONTEND_URL=http://localhost:8080
